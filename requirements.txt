# Python dependencies for victori Flask inference server
# CPU-only PyTorch to reduce image size (Railway 4GB limit)
--extra-index-url https://download.pytorch.org/whl/cpu
Flask==3.1.2
Flask-CORS==6.0.2
torch==2.2.2+cpu
torchvision==0.17.2+cpu
Pillow==10.4.0
